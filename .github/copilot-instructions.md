# Sanctum Theoretica Bot

- Use strict TypeScript and the existing npm scripts.
- Keep Discord secrets in `.env`; never commit `.env` or real tokens.
- Put slash commands in `src/commands` and register them from `src/index.ts`.
- Run `npm run build` after code changes.
